import React from 'react'
import {observer} from 'mobx-react'

function AccountSecuritySectionView() {
    return <>
        <div className="space">
            <div className="dimmed text-small">
                2-step verification provides stronger security for your account.
                To access the account you'll need a password and code generated by the authenticator app on your phone.
            </div>
            <div className="space"/>
            <button className="button" onClick={() => alert('Temporary disabled')}>Setup 2FA</button>
        </div>
        <div className="double-space">
            <div className="dimmed text-small">
                You can change password for your account here. Please note that you should remember new password and
                back up your account because we don't have access to your encrypted data and cannot reset the password.
            </div>
            <div className="space"/>
            <button className="button" onClick={() => alert('Temporary disabled')}>Change password</button>
        </div>
    </>
}
export default observer(AccountSecuritySectionView)